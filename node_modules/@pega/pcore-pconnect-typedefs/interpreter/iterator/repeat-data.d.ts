import { Meta, Options } from '../types';
declare class RepeatDataIterator {
    private readonly meta;
    private readonly datasource;
    private readonly options;
    private index;
    private readonly datasourceObj;
    constructor(meta: Meta, datasource: string, options: Options);
    hasNext(): boolean;
    next(): {
        getPConnect: () => import("../c11n-env").C11nEnv;
    };
    resetIndex(): void;
}
export default RepeatDataIterator;
