import { Meta, Options } from '../types';
import RepeatDataIterator from './repeat-data';
declare class ChildrenIterator {
    private readonly children;
    private readonly options;
    private index;
    constructor(children: any, options: Options);
    hasNext(): boolean;
    next(): {
        getPConnect: () => import("../c11n-env").C11nEnv;
    };
    resetIndex(): void;
}
declare const getChildrenIterator: (meta: Meta, options: Options) => RepeatDataIterator | ChildrenIterator;
export default getChildrenIterator;
