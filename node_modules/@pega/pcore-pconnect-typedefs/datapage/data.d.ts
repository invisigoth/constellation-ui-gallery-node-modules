declare class Data {
    private records;
    private maxRecords;
    private readonly isDirty;
    constructor();
    getRecords(paging: {
        pageNumber: number;
        pageSize: number;
    }): any[];
    mergeRecords(updatedRecords: any[], paging: {
        pageNumber: number;
        pageSize: number;
    }): void;
    setMaxRecordsCount(count: number): void;
    getMaxRecordsCount(): number;
}
export default Data;
