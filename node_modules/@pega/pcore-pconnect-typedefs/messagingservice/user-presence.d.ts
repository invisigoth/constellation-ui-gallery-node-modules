import { MessagePayload } from './types';
declare class UserPresence {
    private users;
    private readonly connected;
    private connectionsCount;
    constructor();
    _handleUserPresence({ message }: MessagePayload): void;
    _getAutoGenId(): string;
    subscribe(userId: string, callback: Function, contextName: string): string;
    unsubscribe(userId: string, subscriptionId: string): void;
    getUserState(userId: string): string;
    clearContext(contextName: string): void;
}
export default UserPresence;
