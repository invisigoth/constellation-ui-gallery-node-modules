declare class ParseUtils {
    static regex: RegExp;
    static subRegex: RegExp;
    static keyMap: {};
    /**
     * @example
     * const localeKey = "Hi {@P .name:'test'}"
     * const result = ParseUtils.formatLocaleKey(localeKey)
     * output:
     * "Hi {@P .name}"
     * @param {string} localeKey
     * @returns string with only prop name
     */
    static formatLocaleKey(localeKey: any): string;
    static getKeyAndValue(match: any): any[];
    static addKeyToMap(key: any, value: any): void;
    /**
     * @example
     * const localeKey = "Ola {@P .name}"
     * const result = ParseUtils.replaceWithValue(localeKey)
     * output:
     * "Ola test"
     * @param {string} localizedValue
     * @returns string by replacing the propName with actual value
     */
    static replaceWithValue(localizedValue: any): string;
}
export default ParseUtils;
