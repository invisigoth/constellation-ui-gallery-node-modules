// Add some context...
/** @deprecated */
const bindAll = (obj, names) => {
    names.forEach(fn => {
        obj[fn] = obj[fn].bind(obj);
    });
};
export default bindAll;
//# sourceMappingURL=bindAll.js.map