const getRelativeOffset = (anchor, el) => {
    const anchorRect = anchor.getBoundingClientRect();
    const elRect = el.getBoundingClientRect();
    const x = anchorRect.x - elRect.x;
    const y = anchorRect.y - elRect.y;
    return { x, y };
};
export default getRelativeOffset;
//# sourceMappingURL=getRelativeOffset.js.map