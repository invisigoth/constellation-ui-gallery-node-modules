const getEdge = ({ el, side, ltr }) => {
    const rect = el.getBoundingClientRect();
    if (side === 'leading') {
        return ltr ? rect.left : rect.right;
    }
    return ltr ? rect.right : rect.left;
};
export default getEdge;
//# sourceMappingURL=getEdge.js.map