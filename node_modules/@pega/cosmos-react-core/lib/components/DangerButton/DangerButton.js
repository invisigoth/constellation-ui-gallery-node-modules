import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import styled from 'styled-components';
import { defaultThemeProp } from '../../theme';
import Button from '../Button';
import Configuration from '../Configuration';
import { useTheme } from '../../hooks';
export const StyledDangerButton = styled(Button) ``;
StyledDangerButton.defaultProps = defaultThemeProp;
const DangerButton = forwardRef(function DangerButton({ label, primary = false, ...restProps }, ref) {
    const theme = useTheme();
    return (_jsx(Configuration, { theme: {
            base: {
                palette: {
                    interactive: theme.base.palette.urgent
                }
            }
        }, children: _jsx(StyledDangerButton, { ...restProps, variant: primary ? 'primary' : undefined, ref: ref, children: label }) }));
});
export default DangerButton;
//# sourceMappingURL=DangerButton.js.map