
/* eslint-disable react/jsx-no-useless-fragment */
// @ts-nocheck
import type { Meta, StoryObj } from '@storybook/react';

import PegaDxilMyPageWidget from './index';


import configProps from './mock';

const meta: Meta<typeof PegaDxilMyPageWidget> = {
  title: 'PegaDxilMyPageWidget',
  component: PegaDxilMyPageWidget,
  excludeStories: /.*Data$/
};

export default meta;
type Story = StoryObj<typeof PegaDxilMyPageWidget>;

export const BasePegaDxilMyPageWidget: Story = args => {

  const props = {
    label: configProps.label,
    header: configProps.header,
    description: configProps.description,
    image: configProps.image,
    datasource: configProps.datasource,
    whatsnewlink: configProps.whatsnewlink
};

return (
    <>
      <PegaDxilMyPageWidget {...props} {...args} />
    </>
  );
};

BasePegaDxilMyPageWidget.args = {
  header: configProps.header,
  description: configProps.description
};
